const fs = require('fs'); const modelsPath = './src/models'; const files = fs.readdirSync(modelsPath); files.forEach(f => { if(f.endsWith('.ts')) { try { const content = fs.readFileSync(modelsPath + '/' + f, 'utf8'); if(content.includes('unique: true')) { console.log(f + ' has unique: true'); const m = content.match(/.*?unique:\s*true.*/g); if(m) console.log(m); } } catch(e){} }});
